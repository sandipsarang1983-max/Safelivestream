package com.example.safe_live_stream_app; // Use your actual package name

import io.flutter.embedding.android.FlutterActivity;

public class MainActivity extends FlutterActivity {
    // Keep this completely empty
}
