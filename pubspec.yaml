dependencies:
  flutter:
    sdk: flutter

  # Firebase packages (Compatible with Flutter 3.6+)
  firebase_core: ^3.9.0
  firebase_auth: ^5.3.4
  cloud_firestore: ^6.4.1
  google_sign_in: ^6.2.2

  # Keep your existing ML Kit and other dependencies
  google_mlkit_object_detection: ^0.12.0
  google_mlkit_commons: ^0.7.0
  google_mlkit_language_id: ^0.12.0
  # ... (keep all other dependencies as you had them)
